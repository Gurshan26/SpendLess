# Gemini API key for AI features (natural language explanations, CSV parsing, what-if analysis)
# Get a free key at https://aistudio.google.com — no credit card required
# Without this, AI features are disabled but everything else works fine
GEMINI_API_KEY=your_key_here
